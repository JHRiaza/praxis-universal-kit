{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PRAXIS Sprint Metrics Schema v0.4",
  "description": "Sprint-level metrics for PRAXIS observational field study. Purely descriptive — no experimental conditions. Supports passive capture, smart checkout, and manual logging with full provenance tracking. v0.4 adds GAS (Governance Activity Score) and steering_intensity.",
  "type": "object",
  "required": [
    "id",
    "timestamp",
    "participant_id",
    "condition",
    "task",
    "duration_minutes",
    "quality_self",
    "schema_version"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique sprint identifier (format: praxis_YYYYMMDDTHHMMSS_slug or auto-generated)"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "When the sprint was logged (UTC, ISO 8601)"
    },
    "schema_version": {
      "type": "string",
      "description": "Schema version this entry conforms to (e.g. '0.3')"
    },
    "participant_id": {
      "type": "string",
      "description": "Pseudonymized participant ID (SHA-256 of hostname+MAC+OS, first 8 chars). NOT anonymous — see consent form."
    },
    "condition": {
      "type": "string",
      "enum": [
        "passive",
        "checked_out",
        "governance_tagged",
        "reviewed"
      ],
      "description": "Capture condition: passive=auto-captured, checked_out=user-calibrated via smart checkout, governance_tagged=includes governance context, reviewed=externally reviewed"
    },
    "phase": {
      "type": "string",
      "enum": ["obs"],
      "description": "Observation phase. Currently always 'obs' (observational). No experimental phases."
    },
    "type": {
      "type": "string",
      "enum": ["sprint", "passive_session"],
      "description": "Entry type: sprint=task-level record, passive_session=session boundary record"
    },
    "task": {
      "type": "string",
      "description": "Description of what was accomplished. SHOULD describe actual work, not tool testing or bug reports."
    },
    "model_executor": {
      "type": ["string", "null"],
      "description": "AI model used. 'unknown' if not detectable from adapters. Free text."
    },
    "model": {
      "type": ["string", "null"],
      "description": "Alias for model_executor (legacy). model_executor is canonical."
    },
    "model_orchestrator": {
      "type": "string",
      "description": "Model orchestrating (if multi-agent setup)"
    },
    "duration_minutes": {
      "type": "integer",
      "minimum": 1,
      "description": "Duration in minutes. For passive capture: wall-clock time between session start/stop, NOT verified active work time."
    },
    "duration": {
      "type": "integer",
      "description": "Alias for duration_minutes (legacy). duration_minutes is canonical."
    },
    "iterations": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of AI generation cycles"
    },
    "quality_self": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "Self-rated quality (1-5). For smart_checkout: derived from checkout_outcome (solved→5, partial→3, abandoned→1). For manual: user-entered."
    },
    "quality": {
      "type": "integer",
      "description": "Alias for quality_self (legacy). quality_self is canonical."
    },
    "quality_external": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "External evaluator quality score (PRAXIS-Q)"
    },
    "quality_evaluator_id": {
      "type": "string",
      "description": "ID of external evaluator (for blind review tracking)"
    },
    "human_interventions": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of human corrections/overrides. Defaults to 0 in passive capture — absence of logged intervention is NOT evidence of autonomous completion."
    },
    "interventions": {
      "type": "integer",
      "description": "Alias for human_interventions (legacy). human_interventions is canonical."
    },
    "autonomous": {
      "type": ["boolean", "null"],
      "description": "DEPRECATED — use governance_activity_score instead. Derived from GAS < 0.3 for backward compatibility. null for passive sessions. Retained for backward compat with v0.11 and earlier."
    },
    "governance_activity_score": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 1,
      "description": "Governance Activity Score (GAS). Composite metric replacing binary 'autonomous'. 0.0=fully autonomous, 1.0=fully human-governed. null for passive sessions where governance activity cannot be determined. Components: correction_density (0.25), tag_weight (0.25), steering_proxy (0.25), skepticism_signal (0.25)."
    },
    "gas_components": {
      "type": ["object", "null"],
      "description": "Decomposed GAS sub-scores for analysis. null for passive sessions.",
      "properties": {
        "correction_density": { "type": "number", "minimum": 0, "maximum": 1, "description": "interventions / max(iterations, 1), capped at 1.0" },
        "tag_weight": { "type": "number", "minimum": 0, "maximum": 0.3, "description": "0.3 if governance_tag != 'none', else 0.0" },
        "steering_proxy": { "type": "number", "minimum": 0, "maximum": 1, "description": "steering_intensity (1-5) normalized to 0.0-1.0" },
        "skepticism_signal": { "type": "number", "minimum": 0, "maximum": 1, "description": "l1r_observations.skepticism_activation / 7" }
      }
    },
    "first_attempt": {
      "type": "boolean",
      "description": "Was the first output accepted?"
    },
    "corrections": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of corrections applied"
    },
    "praxis_layer": {
      "type": "string",
      "enum": ["L1", "L1-R", "L2", "L3", "L4", "L5"],
      "description": "Dominant PRAXIS layer"
    },
    "outputs": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Artifacts produced"
    },
    "platforms": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Detected AI platforms. NOTE: detects INSTALLED platforms, not necessarily platforms ACTIVE during this session."
    },
    "session_id": {
      "type": "string",
      "description": "Short session hash linking this sprint to its passive capture session"
    },
    "reviewed": {
      "type": "boolean",
      "description": "Whether this entry has been user-reviewed via checkout"
    },
    "governance_tag": {
      "type": "string",
      "description": "Governance context tag (e.g. 'none', 'rule_created', 'incident_logged')"
    },

    "capture_mode": {
      "type": "string",
      "enum": ["passive_auto", "smart_checkout", "manual"],
      "description": "How this entry was captured. passive_auto=automatic session detection. smart_checkout=user-calibrated. manual=explicit logging."
    },
    "source_session_id": {
      "type": "string",
      "description": "ID of the passive capture session this sprint was derived from"
    },
    "checkout_outcome": {
      "type": "string",
      "enum": ["solved", "partial", "abandoned"],
      "description": "User's assessment of the sprint outcome via smart checkout"
    },

    "field_provenance": {
      "type": "object",
      "description": "Tracks the origin of each field value. Keys are field names, values are: 'auto' (detected), 'smart_checkout' (user-confirmed), 'manual' (user-entered), 'unknown' (not determinable).",
      "additionalProperties": {
        "type": "string",
        "enum": ["auto", "smart_checkout", "manual", "unknown"]
      }
    },
    "provenance_completeness": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Fraction of fields with non-'unknown' provenance. Replaces legacy 'reliability_score'. NOT a measure of data quality — measures capture completeness only."
    },
    "reliability_score": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "DEPRECATED — alias for provenance_completeness. Retained for backward compatibility."
    },

    "passive_capture": {
      "type": "object",
      "description": "Passive capture session telemetry",
      "properties": {
        "started_at": { "type": "string", "format": "date-time" },
        "ended_at": { "type": ["string", "null"], "format": "date-time" },
        "platform_ids": { "type": "array", "items": { "type": "string" } },
        "git_start": { "$ref": "#/$defs/git_snapshot" },
        "git_end": { "$ref": "#/$defs/git_snapshot" },
        "signals": {
          "type": "object",
          "properties": {
            "platform_count": { "type": "integer" },
            "git_repo_detected": { "type": "boolean" },
            "dirty_files_end": { "type": ["integer", "null"] },
            "git_commit_delta": { "type": "integer" }
          }
        }
      }
    },

    "l1r_observations": {
      "type": "object",
      "description": "L1-R Relational governance observations. IMPORTANT: fields may be directly observed (participant-rated) or algorithmically derived from checkout_outcome. Check l1r_source to determine which.",
      "properties": {
        "steering_intensity": {
          "type": "integer", "minimum": 1, "maximum": 5,
          "description": "How much did you steer/guide the AI during this session? (1=none, 5=constant) — v0.12.0+"
        },
        "trust_willingness": {
          "type": "integer", "minimum": 1, "maximum": 7,
          "description": "Would you follow this AI's advice without verifying? (Likert 1-7)"
        },
        "skepticism_activation": {
          "type": "integer", "minimum": 1, "maximum": 7,
          "description": "Did you feel the need to verify independently? (Likert 1-7)"
        },
        "perceived_confidence": {
          "type": "integer", "minimum": 1, "maximum": 7,
          "description": "How confident did the AI seem? (Likert 1-7)"
        },
        "perceived_warmth": {
          "type": "integer", "minimum": 1, "maximum": 7,
          "description": "How warm/supportive did the AI feel? (Likert 1-7)"
        },
        "perceived_authority": {
          "type": "integer", "minimum": 1, "maximum": 7,
          "description": "How expert did the AI seem? (Likert 1-7)"
        },
        "compliance_tendency": {
          "type": "boolean",
          "description": "Did you accept the AI's output without questioning?"
        },
        "personality_mismatch": {
          "type": "boolean",
          "description": "Did the AI's behavior differ from what was expected?"
        },
        "personality_mismatch_notes": {
          "type": "string",
          "description": "Description of any mismatch observed"
        }
      }
    },
    "l1r_source": {
      "type": "string",
      "enum": ["observed", "derived", "mixed", "unknown"],
      "description": "CRITICAL: Whether L1-R values were directly observed (participant explicitly rated) or derived (computed from checkout_outcome). 'derived' values are NOT psychological measurements and should not be analyzed as Likert-scale data."
    },

    "governance_events": {
      "type": "array",
      "description": "Governance emergence events logged during this sprint",
      "items": {
        "type": "object",
        "properties": {
          "event_type": {
            "type": "string",
            "enum": ["rule_created", "rule_modified", "rule_pruned", "incident", "escalation"]
          },
          "description": { "type": "string" },
          "root_cause": { "type": "string" },
          "new_rule": { "type": "string" },
          "timestamp": { "type": "string", "format": "date-time" },
          "incident_category": {
            "type": "string",
            "enum": ["OPS", "GOV", "COM", "PRD", "RES", "DES"],
            "description": "Incident domain. DES = design/writing/game-design failures."
          }
        }
      }
    },

    "session_boundary": {
      "type": "object",
      "description": "Session boundary observation (if applicable)",
      "properties": {
        "memory_recovery": {
          "type": "string",
          "enum": ["instant", "partial", "lost"],
          "description": "How quickly did stored memory recover across session boundary?"
        },
        "calibration_recovery": {
          "type": "string",
          "enum": ["immediate", "gradual", "significant_degradation"],
          "description": "How well did execution calibration recover?"
        },
        "notes": { "type": "string" }
      }
    },

    "iteration_type": {
      "type": "string",
      "enum": ["implementation", "debug", "refactor", "research", "design_cycle", "playtest", "revision", "refinement"],
      "description": "Optional subtype of iteration"
    },

    "design_quality": {
      "type": "object",
      "description": "Creative/design quality sub-metrics",
      "properties": {
        "clarity": { "type": "integer", "minimum": 1, "maximum": 5 },
        "tension": { "type": "integer", "minimum": 1, "maximum": 5 },
        "balance": { "type": "integer", "minimum": 1, "maximum": 5 },
        "elegance": { "type": "integer", "minimum": 1, "maximum": 5 },
        "notes": { "type": "string" }
      }
    },

    "reviewer_feedback": {
      "type": "object",
      "description": "Optional external evaluation notes",
      "properties": {
        "reviewer_id": { "type": "string" },
        "source": { "type": "string" },
        "summary": { "type": "string" },
        "action_items": { "type": "array", "items": { "type": "string" } },
        "sentiment": { "type": "string", "enum": ["positive", "mixed", "negative"] }
      }
    },

    "notes": {
      "type": "string",
      "description": "Free-text notes about this sprint"
    },
    "delegation_depth": {
      "type": "integer",
      "minimum": 0,
      "maximum": 2,
      "description": "How many layers of delegation: 0=direct human-AI, 1=delegated via orchestrator, 2=multi-hop delegation"
    },
    "context_provision_effort": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "How much context did the human provide upfront? (1=minimal, 5=extensive)"
    },
    "decision_latency_seconds": {
      "type": ["number", "null"],
      "description": "Seconds between session end and checkout completion. Proxy for deliberation time."
    },
    "heuristic_analysis": {
      "type": ["object", "null"],
      "description": "Rule-based heuristic governance detection results (Layer 1 of cross-validation stack)",
      "properties": {
        "signals": { "type": "array", "items": { "type": "string" } },
        "signal_count": { "type": "integer" },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "details": { "type": "object" },
        "heuristic_version": { "type": "string" }
      }
    },
    "cross_validation": {
      "type": ["object", "null"],
      "description": "Cross-validation results comparing heuristic (L1) vs LLM judge (L2) governance detection",
      "properties": {
        "heuristic_signals": { "type": "array", "items": { "type": "string" } },
        "judge_signals": { "type": "array", "items": { "type": "string" } },
        "agreement_signals": { "type": "array", "items": { "type": "string" } },
        "agreement_ratio": { "type": "number", "minimum": 0, "maximum": 1 },
        "judge_result": { "type": "object" },
        "cross_validation_version": { "type": "string" }
      }
    }
  },

  "$defs": {
    "git_snapshot": {
      "type": "object",
      "description": "Git repository state at a point in time",
      "properties": {
        "repo": { "type": "boolean", "description": "Whether a git repo was detected" },
        "commit": { "type": ["string", "null"], "description": "Short commit hash" },
        "branch": { "type": ["string", "null"], "description": "Current branch name" },
        "dirty_files": { "type": ["integer", "null"], "description": "Number of uncommitted changes" },
        "git_unavailable": { "type": "boolean", "description": "True if git was not found on this system (no CLT on macOS)" }
      }
    }
  }
}
